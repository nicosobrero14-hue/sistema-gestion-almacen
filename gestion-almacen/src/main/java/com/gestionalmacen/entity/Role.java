package com.gestionalmacen.entity;

// Roles del sistema (RF-11). Los valores son los mismos que se guardan en la base.
public enum Role {
	ADMIN,    // el dueño: acceso a todo
	EMPLEADO  // ventas, productos, stock y proveedores
}
